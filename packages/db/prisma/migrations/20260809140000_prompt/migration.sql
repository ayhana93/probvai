-- Текстът, с който човекът насочва генерацията.
--
-- Пише се при създаването на пробата — от системната роля, в същата
-- транзакция като удържането на кредита. `app_user` няма нужда от право за
-- писане: той не пипа реда след създаването му.

ALTER TABLE "generations" ADD COLUMN "prompt" TEXT;
