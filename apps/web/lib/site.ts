/**
 * ПУБЛИЧНИЯТ АДРЕС НА САЙТА
 *
 * ═══ ЗАЩО Е ТУК, А НЕ В СРЕДАТА ═══
 *
 * Билдът на този проект нарочно не чете нищо от средата (`npm run build:clean`
 * го проверява). А `metadataBase` трябва да е известен ИМЕННО при билда:
 * статичните страници се изписват тогава и в тях се запича пълният адрес на
 * картинката за споделяне.
 *
 * Прочетен от `process.env` при билд, на празна среда той би станал
 * `localhost:3000` — и всяка мрежа, която дърпа картинката, би получила
 * нищо. Тихо: линкът се показва, само без картинка.
 *
 * Затова адресът е КОД. Той не е настройка на средата — той е свойство на
 * продукта: домейнът е един и същ, независимо къде върви образът.
 *
 * ═══ КОГА СЕ ПИПА ═══
 *
 * При смяна на домейна. Тогава се сменя И `APP_URL` в средата — оттам идват
 * адресите в имейлите и връщането от Stripe.
 */
export const SITE_URL = 'https://probvai-production.up.railway.app';

/** Каквото се вижда до линка при споделяне. */
export const SITE_NAME = 'ПРОБВАЙ';
export const SITE_TAGLINE = 'Виж как ти стои дрехата, преди да я поръчаш.';
